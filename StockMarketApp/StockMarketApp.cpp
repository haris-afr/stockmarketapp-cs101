#include <SFML/Graphics.hpp>
using namespace std;

int main()
{
    //creates a window and titles it 
    sf::RenderWindow window(sf::VideoMode(600, 480), "Stock Market App", sf::Style::Close || sf::Style::Titlebar);
    return 0;

    while (window.isOpen()) {

    }
}